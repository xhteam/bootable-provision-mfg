u-boot-6q.bin -> uboot bootloader
boot.img -> kernel+ramdisk
system.img ->standard android system image
recovery.img ->kernel+recovery ramdisk

For Quester board,another splash image required.
splash.img ->splash image+other built in images used by uboot bootloader.
